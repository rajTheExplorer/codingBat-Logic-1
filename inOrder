public boolean inOrder(int a, int b, int c, boolean bOk) {
  if(bOk==false)
  {
    if((b>a) && (c>b))
    return true;
    else
    return false;
  }
  else 
  {
    if(c>b)
    return true;
    else
    return false;
  }
  
}
